# Use a node_modules folder (like npm) instead of Plug'n'Play, and run
# dependency build scripts (sharp, @swc/core, esbuild, ...) so native
# binaries are built — matching the previous npm behavior.
nodeLinker: node-modules
enableScripts: true
